<nav class="bg-gray-900 shadow-2xl px-8 py-4 my-4 rounded flex items-center justify-between">
    <div>
        <a href="{{ path('home') }}"><h2 class="text-2xl font-bold text-blue-300">Labo 02</h2></a>
    </div>
    <div class="flex">
        {% if app.user is null %}
            <div class="hover:text-blue-300 font-bold tracking-wide mr-4">
                <a href="{{ path('registration') }}" class="cursor-pointer">
                    <span class="hidden sm:inline-block">Inscription</span>
                    <span class="block sm:hidden">
                        <i class="material-icons">
                            person_add
                        </i>
                    </span>
                </a>
            </div>
            <div class="hover:text-blue-300 font-bold tracking-wide">
                <a href="{{ path('login') }}" class="cursor-pointer">
                    <span class="hidden sm:inline-block">Connexion</span>
                    <span class="block sm:hidden">
                        <i class="material-icons">
                            person
                        </i>
                    </span>
                </a>
            </div>
        {% else %}
            <div class="hover:text-blue-300 font-bold tracking-wide">
                <a href="/logout" class="cursor-pointer">
                    <span class="hidden sm:inline-block">Déconnexion</span>
                    <span class="block sm:hidden">
                        <i class="material-icons">
                            person
                        </i>
                    </span>
                </a>
            </div>
        {% endif %}
    </div>
</nav>